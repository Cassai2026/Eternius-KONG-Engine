# Supercharge Kit

A lightweight starter framework for organizing engine features into reusable systems and services.

The runtime now bootstraps the shared SQLite `build_menu` schema so mesh and HUD flows can start from an empty database.
